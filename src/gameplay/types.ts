import { ButtonBonus, Field, Panel, ProgressBar, Wallet } from '/components';

export type TGameplayContent = {
  field: Field;
  progressBar: ProgressBar;
  panel: Panel;
  wallet: Wallet;
  buttonBonusBomb: ButtonBonus;
  buttonBonusShuffle: ButtonBonus;
  buttonBonusVertical: ButtonBonus;
  buttonBonusHorizontal: ButtonBonus;
};
